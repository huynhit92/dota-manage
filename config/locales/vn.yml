vn:
  views:
    pagination:
      # first: "&laquo; 最初"
      # last: "最終 &raquo;"
      # previous: "&lsaquo; 前へ"
      # next: "次へ &rsaquo;"
      # truncate: "..."
  activerecord:
    models:
      

    attributes:
      defaults: &defaults
        